import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#BDBDBD",
    },
    scrollContent: {
        padding: 20,
        paddingTop: 60,
    },
    header: {
        alignItems: "center",
        marginBottom: 24,
    },
    titulo: {
        fontSize: 28,
        color: "#fff",
        marginBottom: 8,
    },
    subtitulo: {
        fontSize: 18,
        color: "#fff",
        opacity: 0.9,
    },
});